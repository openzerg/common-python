[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "openzerg-common"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "connectrpc>=0.9.0",
    "protobuf>=7.34.1",
]

[tool.setuptools.packages.find]
where = ["src"]
